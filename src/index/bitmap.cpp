#include <string>
#include <bitset>
#include "../ad/advertising.hpp"

using namespace std;

void init_bitmap(vector<Adv> &ads) {
    for (int i = 0; i < ads.size(); i++) {
        Adv ad = ads[i];
        
    }
}